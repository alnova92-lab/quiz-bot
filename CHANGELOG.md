# История изменений

## [1.0.0] - 2024-02-10

### Добавлено
- ✅ Полная реализация бота-квиза для Telegram
- ✅ Логика "1 день = 1 вопрос" с автоматической сменой при наступлении 00:00
- ✅ Защита от ответов на устаревшие вопросы
- ✅ Автоматическая очистка сессий при смене дня
- ✅ Система баллов с бонусами за скорость и топ-50
- ✅ Еженедельная рассылка рейтинга
- ✅ База вопросов в Excel с импортом в SQLite
- ✅ Планировщик публикаций в случайное время (9:00-21:00)

### Файлы
- `bot.py` - главный файл бота
- `database.py` - работа с SQLite базой данных
- `scheduler.py` - планировщик публикаций
- `config.py` - настройки
- `handlers.py` - обработчики команд
- `quiz_logic.py` - логика квиза с защитой от устаревших вопросов
- `import_questions.py` - импорт вопросов из Excel
- `create_sample_excel.py` - создание примера базы вопросов
- `test_day_logic.py` - тестирование логики смены дня
- `DAILY_LOGIC.md` - подробная документация механизма "1 день = 1 вопрос"
- `README.md` - инструкция по установке и использованию

### Особенности реализации

#### Механизм "1 день = 1 вопрос"

**Проблема:** Как обеспечить, что при наступлении 00:00 старый вопрос автоматически становится неактивным?

**Решение:**
1. В таблице `daily_questions` используется поле `publish_date` (только дата)
2. Метод `get_today_question()` фильтрует по `WHERE publish_date = CURRENT_DATE`
3. При смене даты SQL автоматически возвращает новый вопрос
4. Не требуется cron-задач для деактивации

**Защита от краевых случаев:**
```python
# Пользователь открыл вопрос в 23:59, ответил в 00:01
if question['id'] != daily_question_id:
    return "Этот вопрос уже неактивен!"
```

**Очистка сессий:**
```python
def cleanup_expired_sessions(self):
    today_question = self.db.get_today_question()
    # Удаляем все сессии, кроме сегодняшней
    expired_users = [
        user_id for user_id, q_id in self.active_sessions.items()
        if q_id != today_question['id']
    ]
    for user_id in expired_users:
        del self.active_sessions[user_id]
```

Вызывается перед каждым:
- `start_quiz_for_user()`
- `check_answer()`

#### Система баллов

- **5 баллов** — базовые за правильный ответ
- **+5 баллов** — бонус для первых 50 правильных ответов
- **+2 балла** — бонус за скорость (≤10 секунд)

Бонусы стакаются: максимум **12 баллов** за один ответ.

#### Планировщик

- Генерирует случайное время публикации в диапазоне 9:00-21:00
- После публикации автоматически планирует следующий вопрос
- Еженедельная рассылка рейтинга (воскресенье, 20:00)

### Тестирование

Запустите `python test_day_logic.py` для проверки:
- ✅ Активен только вопрос текущего дня
- ✅ Блокировка ответов на вопросы прошлых дней
- ✅ Проверка `check_user_answered_today()`
- ✅ Автоматическая смена активного вопроса при 00:00

### Требования

- Python 3.10+
- python-telegram-bot 21.9
- APScheduler 3.10.4
- pandas 2.2.0
- openpyxl 3.1.2
- python-dotenv 1.0.0

### Установка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Создание .env с токенами
cp .env.example .env
# Отредактируйте .env

# Создание базы вопросов
python create_sample_excel.py
# Отредактируйте questions.xlsx

# Импорт вопросов
python import_questions.py

# Запуск бота
python bot.py
```

### Документация

- 📖 [README.md](README.md) - основная документация
- 📖 [DAILY_LOGIC.md](DAILY_LOGIC.md) - механизм "1 день = 1 вопрос"
- 📖 [quiz_bot_full_plan.md](quiz_bot_full_plan.md) - полное техническое задание

---

## Автор

Создано на основе технического задания "Областная газета"
