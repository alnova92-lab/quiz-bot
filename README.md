# Бот-Квиз "Областная Газета"

Telegram-бот для проведения ежедневных викторин в канале.

## Особенности

- **1 день = 1 вопрос** — при наступлении 00:00 старый вопрос автоматически деактивируется
- Ежедневная публикация вопросов в случайное время (9:00-21:00)
- Система баллов с бонусами за скорость и позицию
- Еженедельный рейтинг участников
- База вопросов в Excel для простого редактирования
- SQLite база данных для хранения статистики
- Защита от ответов на устаревшие вопросы

## Структура проекта

```
QUIZ_bot/
├── bot.py                      # Главный файл бота
├── database.py                 # Работа с базой данных
├── scheduler.py                # Планировщик публикаций
├── config.py                   # Настройки
├── handlers.py                 # Обработчики команд
├── quiz_logic.py               # Логика квиза
├── import_questions.py         # Импорт вопросов из Excel
├── create_sample_excel.py      # Создание примера questions.xlsx
├── requirements.txt            # Зависимости Python
├── .env.example                # Пример файла с токенами
└── README.md                   # Этот файл
```

## Установка и настройка

### 1. Установите Python 3.10+

Скачайте с [python.org](https://python.org) и установите.

### 2. Создайте бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Введите название и username бота
4. Сохраните токен

### 3. Получите ID канала

Для публичного канала:
```
@ваш_канал
```

Для приватного канала:
1. Добавьте бота в канал как администратора
2. Отправьте сообщение в канал
3. Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
4. Найдите `"chat":{"id":-100xxxxxxxxx}`

### 4. Настройте проект

Создайте файл `.env` (скопируйте из `.env.example`):

```env
BOT_TOKEN=ваш_токен_от_BotFather
CHANNEL_ID=@ваш_канал
CHANNEL_USERNAME=ваш_канал
```

### 5. Установите зависимости

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 6. Инициализируйте базу данных

```bash
# Создать базу данных quiz.db с таблицами
python init_database.py
```

**Примечание:** База данных также создается автоматически при первом запуске `bot.py` или `import_questions.py`, но рекомендуется создать её явно.

### 7. Создайте базу вопросов

```bash
# Создать пример questions.xlsx с 5 вопросами
python create_sample_excel.py

# Откройте questions.xlsx в Excel и добавьте свои вопросы
# Затем импортируйте их в базу данных:
python import_questions.py
```

### 8. Запустите бота

```bash
python bot.py
```

## Формат вопросов в Excel

| Категория | Вопрос | Вариант А | Вариант Б | Вариант В | Вариант Г | Правильный | Объяснение | Картинка (URL) |
|-----------|--------|-----------|-----------|-----------|-----------|------------|------------|----------------|
| История | Текст вопроса | Ответ 1 | Ответ 2 | Ответ 3 | Ответ 4 | Б | Объяснение правильного ответа | (необязательно) |

**Важно:** Правильный ответ указывается буквой: А, Б, В или Г

## Команды бота

- `/start` - Начало работы с ботом
- `/rating` - Посмотреть текущий рейтинг
- `/stats` - Посмотреть свою статистику
- `/help` - Справка

## Система баллов

- **5 баллов** — за правильный ответ
- **+5 баллов** — бонус для первых 50 правильных ответов (топ-50)
- **+2 балла** — бонус за скорость (ответ в течение 10 секунд)

### Примеры начисления:

- Ответил правильно, №23, за 7 сек = **12 баллов** (5 + 5 + 2)
- Ответил правильно, №23, за 15 сек = **10 баллов** (5 + 5)
- Ответил правильно, №67, за 8 сек = **7 баллов** (5 + 2)
- Ответил правильно, №67, за 15 сек = **5 баллов** (5)
- Ответил неправильно = **0 баллов**

## Настройки (config.py)

```python
PUBLISH_TIME_START = 9      # Начало диапазона публикации
PUBLISH_TIME_END = 21       # Конец диапазона публикации
POINTS_CORRECT = 5          # Баллы за правильный ответ
POINTS_TOP50 = 5            # Бонус за топ-50
POINTS_SPEED = 2            # Бонус за скорость
SPEED_LIMIT = 10            # Лимит скорости (секунды)
RATING_TOP_N = 10           # Количество мест в рейтинге
RATING_SEND_DAY = 6         # День рассылки (6 = воскресенье)
```

## Автозапуск

### Windows (Task Scheduler)

1. Откройте "Планировщик заданий"
2. Создать задачу → Общие: "Quiz Bot"
3. Триггеры: "При запуске компьютера"
4. Действия:
   - Программа: `C:\путь\к\venv\Scripts\python.exe`
   - Аргументы: `bot.py`
   - Рабочая папка: `C:\путь\к\QUIZ_bot`

### Linux (systemd)

Создайте файл `/etc/systemd/system/quizbot.service`:

```ini
[Unit]
Description=Quiz Bot
After=network.target

[Service]
Type=simple
User=ваш_username
WorkingDirectory=/home/ваш_username/QUIZ_bot
ExecStart=/home/ваш_username/QUIZ_bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable quizbot
sudo systemctl start quizbot
```

## Добавление новых вопросов

1. Откройте `questions.xlsx`
2. Добавьте новые строки с вопросами
3. Сохраните файл
4. Запустите: `python import_questions.py`

Вопросы добавятся в базу, не удаляя старые.

## Решение проблем

### Бот не запускается

```bash
# Проверьте токен
python -c "from config import BOT_TOKEN; print(BOT_TOKEN)"

# Обновите зависимости
pip install -r requirements.txt --upgrade
```

### Вопросы не публикуются

- Убедитесь, что бот добавлен в канал как администратор
- Проверьте CHANNEL_ID в .env
- Проверьте логи на ошибки

### База данных

База данных SQLite хранится в файле `quiz.db`. Для резервного копирования просто скопируйте этот файл.

## Структура базы данных

- **users** — пользователи и их статистика
- **questions** — база вопросов
- **daily_questions** — опубликованные вопросы (с `publish_date` для фильтрации по дням)
- **answers** — ответы пользователей
- **weekly_ratings** — история рассылок рейтинга

## Логика "1 день = 1 вопрос"

**Важно:** Бот автоматически обеспечивает, что в каждый день активен только один вопрос.

- При наступлении 00:00 старый вопрос автоматически становится неактивным
- Пользователь не может ответить на вопросы прошлых дней
- Пользователь не может ответить дважды на один и тот же вопрос
- Все сессии автоматически очищаются при смене дня

📖 Подробности в файле [DAILY_LOGIC.md](DAILY_LOGIC.md)

🧪 Тестирование: `python test_day_logic.py`

## Лицензия

MIT

## Поддержка

Если возникли вопросы, проверьте:
1. Файл `quiz_bot_full_plan.md` — полная документация
2. Логи бота в консоли
3. Права бота в канале

Удачи с запуском квиза!
