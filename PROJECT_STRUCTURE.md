# Структура проекта

## Основные файлы бота

### bot.py
**Главный файл приложения**
- Запуск бота
- Регистрация обработчиков команд
- Настройка планировщика заданий
- Инициализация базы данных

```bash
python bot.py
```

### database.py
**Работа с SQLite базой данных**

Методы:
- `init_database()` - создание таблиц
- `add_user()` - регистрация пользователя
- `get_user()` - получение данных пользователя
- `add_question()` - добавление вопроса
- `get_random_unused_question()` - случайный неиспользованный вопрос
- `create_daily_question()` - создание дневного вопроса
- `get_today_question()` - **получение СЕГОДНЯШНЕГО вопроса**
- `check_user_answered_today()` - **проверка ответа СЕГОДНЯ**
- `save_answer()` - сохранение ответа
- `get_rating()` - получение рейтинга
- `get_user_rank()` - позиция пользователя
- `get_all_active_users()` - все активные пользователи

### quiz_logic.py
**Логика квиза и система баллов**

Методы:
- `cleanup_expired_sessions()` - **очистка устаревших сессий**
- `start_quiz_for_user()` - начать квиз для пользователя
- `check_answer()` - **проверка ответа с защитой от устаревших вопросов**

**Ключевая логика:**
```python
# Защита от ответов на устаревшие вопросы
if question['id'] != daily_question_id:
    return "Этот вопрос уже неактивен!"
```

### handlers.py
**Обработчики команд Telegram**

Команды:
- `/start` - приветствие и регистрация
- `/rating` - показать рейтинг
- `/stats` - статистика пользователя
- `/help` - справка

Callback-обработчики:
- `show_question()` - показать вопрос
- `answer_question_callback()` - показать вопрос (из callback)
- `answer_callback()` - обработка ответа

**Важно:** Все обработчики вызывают `cleanup_expired_sessions()` перед работой.

### scheduler.py
**Планировщик публикаций**

Методы:
- `get_random_publish_time()` - генерация случайного времени (9-21ч)
- `publish_daily_question()` - публикация вопроса в канале
- `send_weekly_ratings()` - еженедельная рассылка рейтинга

### config.py
**Настройки бота**

Параметры:
```python
BOT_TOKEN              # Токен бота
CHANNEL_ID             # ID канала
CHANNEL_USERNAME       # Username канала
PUBLISH_TIME_START     # 9 (начало диапазона)
PUBLISH_TIME_END       # 21 (конец диапазона)
POINTS_CORRECT         # 5 баллов
POINTS_TOP50           # +5 баллов
POINTS_SPEED           # +2 балла
SPEED_LIMIT            # 10 секунд
RATING_TOP_N           # 10 мест
RATING_SEND_DAY        # 6 (воскресенье)
```

---

## Вспомогательные скрипты

### import_questions.py
**Импорт вопросов из Excel в БД**

```bash
python import_questions.py
```

Читает `questions.xlsx` и добавляет вопросы в базу данных.

### create_sample_excel.py
**Создание примера questions.xlsx**

```bash
python create_sample_excel.py
```

Создает файл с 5 примерами вопросов.

### test_day_logic.py
**Тестирование логики "1 день = 1 вопрос"**

```bash
python test_day_logic.py
```

Проверяет:
- Активен только сегодняшний вопрос
- Блокировка вопросов прошлых дней
- Функцию `check_user_answered_today()`
- Автоматическую смену при 00:00

---

## Конфигурационные файлы

### .env.example
**Пример файла с токенами**

Скопируйте в `.env` и заполните:
```env
BOT_TOKEN=your_token_here
CHANNEL_ID=@your_channel
CHANNEL_USERNAME=your_channel
```

### requirements.txt
**Зависимости Python**

```bash
pip install -r requirements.txt
```

---

## Документация

### README.md
**Основная документация**
- Установка и настройка
- Команды бота
- Система баллов
- Автозапуск
- Решение проблем

### DAILY_LOGIC.md
**Подробная документация механизма "1 день = 1 вопрос"**

Разделы:
1. Как это работает
2. Реализация (database.py, quiz_logic.py, handlers.py)
3. Сценарии работы
4. Тестирование
5. Выводы

**Обязательно прочитайте!** Объясняет:
- Почему не нужны cron-задачи
- Как работает автоматическая смена при 00:00
- Защиту от краевых случаев
- Очистку сессий

### CHANGELOG.md
**История изменений**
- Список всех функций
- Особенности реализации
- Требования
- Инструкция по установке

### PROJECT_STRUCTURE.md
**Этот файл**
- Описание всех файлов проекта
- Назначение каждого модуля
- Примеры использования

### quiz_bot_full_plan.md
**Полное техническое задание**
- Механика работы
- Архитектура системы
- Структура БД
- Полный код всех модулей
- Инструкция по запуску

---

## База данных

### quiz.db
**SQLite база данных**

Таблицы:
- `users` - пользователи и статистика
- `questions` - вопросы
- `daily_questions` - **опубликованные вопросы (с publish_date)**
- `answers` - ответы пользователей
- `weekly_ratings` - история рассылок

**Важно:** Поле `publish_date` в таблице `daily_questions` — ключ к механизму "1 день = 1 вопрос".

---

## Данные

### questions.xlsx
**База вопросов (Excel)**

Столбцы:
- Категория
- Вопрос
- Вариант А
- Вариант Б
- Вариант В
- Вариант Г
- Правильный (А/Б/В/Г)
- Объяснение
- Картинка (URL) - необязательно

---

## Дерево файлов

```
QUIZ_bot/
├── 📄 bot.py                      # Главный файл
├── 📄 database.py                 # Работа с БД
├── 📄 scheduler.py                # Планировщик
├── 📄 config.py                   # Настройки
├── 📄 handlers.py                 # Обработчики команд
├── 📄 quiz_logic.py               # Логика квиза
├── 📄 import_questions.py         # Импорт из Excel
├── 📄 create_sample_excel.py      # Создание примера Excel
├── 📄 test_day_logic.py           # Тест логики дня
│
├── 📄 requirements.txt            # Зависимости
├── 📄 .env.example                # Пример токенов
├── 📄 .env                        # Ваши токены (создайте!)
│
├── 📖 README.md                   # Основная документация
├── 📖 DAILY_LOGIC.md              # Логика "1 день = 1 вопрос"
├── 📖 CHANGELOG.md                # История изменений
├── 📖 PROJECT_STRUCTURE.md        # Этот файл
├── 📖 quiz_bot_full_plan.md       # Полное ТЗ
│
├── 💾 quiz.db                     # База данных (создается автоматически)
└── 📊 questions.xlsx              # База вопросов (создайте!)
```

---

## Порядок запуска

### Первый запуск

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Создайте .env:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env
   ```

3. **Создайте базу вопросов:**
   ```bash
   python create_sample_excel.py
   # Откройте questions.xlsx и добавьте вопросы
   ```

4. **Импортируйте вопросы:**
   ```bash
   python import_questions.py
   ```

5. **Протестируйте логику:**
   ```bash
   python test_day_logic.py
   ```

6. **Запустите бота:**
   ```bash
   python bot.py
   ```

### Обычная работа

```bash
# Запуск бота
python bot.py

# Добавление новых вопросов
# 1. Откройте questions.xlsx
# 2. Добавьте строки
# 3. Запустите:
python import_questions.py
```

---

## Ключевые моменты

### ✅ Логика "1 день = 1 вопрос" РЕАЛИЗОВАНА

**Механизм:**
1. `get_today_question()` использует `WHERE publish_date = CURRENT_DATE`
2. При наступлении 00:00 автоматически меняется результат SQL-запроса
3. Защита от ответов на устаревшие вопросы в `check_answer()`
4. Автоматическая очистка сессий в `cleanup_expired_sessions()`

**Не требуется:**
- ❌ Cron-задачи для деактивации
- ❌ Ручная смена вопросов
- ❌ Дополнительные флаги активности

**Все работает автоматически через SQL и `date.today()`!**

### 📖 Подробности

Прочитайте [DAILY_LOGIC.md](DAILY_LOGIC.md) для полного понимания механизма.

---

## Вопросы?

- 📖 Основная информация: [README.md](README.md)
- 🔧 Логика дня: [DAILY_LOGIC.md](DAILY_LOGIC.md)
- 📋 История: [CHANGELOG.md](CHANGELOG.md)
- 📄 Полное ТЗ: [quiz_bot_full_plan.md](quiz_bot_full_plan.md)

Удачи с запуском! 🍀
